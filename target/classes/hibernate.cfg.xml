<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
      "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
      "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
  <session-factory>
    <!-- Configuración de conexión a la base de datos H2 en memoria -->
    <property name="connection.driver_class">org.h2.Driver</property>
    <property name="connection.url">jdbc:h2:mem:test;DB_CLOSE_DELAY=-1</property>
    <property name="connection.username">sa</property>
    <property name="connection.password"></property>

    <!-- Dialecto de la base de datos H2 -->
    <property name="dialect">org.hibernate.dialect.H2Dialect</property>

    <!-- Mostrar las sentencias SQL en consola -->
    <property name="show_sql">true</property>

    <!-- Genera/actualiza el esquema automáticamente -->
    <property name="hbm2ddl.auto">update</property>

    <!-- Mapeo de las entidades -->
    <mapping class="com.mig.personal.correoApp.Correo"/>
    <mapping class="com.mig.personal.correoApp.TipoCorreo"/>
  </session-factory>
</hibernate-configuration>
